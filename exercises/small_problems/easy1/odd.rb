# make an is_odd? method that will work with all integers.
# can't use #odd? or #even?

def is_odd?(integer)
  integer%2 != 0
end

# % (modulo) isn't always a remainder operator in other languages.