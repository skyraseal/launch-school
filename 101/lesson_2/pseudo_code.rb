# pseudo_code

# A method that returns the sum of two Integers
=begin
  Ask for the first integer
  SET number1 = answer
  Ask for the second integer
  SET number2 = answer
  
  SET result = number1 + number2
  PRINT result
=end

# a method that takes an array of strings, and returns a string that is all those strings concatenated together
=begin 
  define a method that takes in one parameter
  check parameter to be an array
    if not an array, break and return nil
  create an empty string
  iterate through the array and add each object to the string
  return the string value
=end

# a method that takes an array of integers, and returns a new array with every other element 
=begin 
  define a method that takes in one parameter
  check parameter to be an array
    if not an array, break and return nil
  if index of the item % 2 ==0, select that object into a new array
=end
